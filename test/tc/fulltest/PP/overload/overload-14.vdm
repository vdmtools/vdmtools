class A

operations

op : nat * nat ==> bool
op(-,-) ==
  return true;

op : nat * real ==> bool
op(-,-) ==
  return true;

op2 : nat * nat ==> bool
op2(-,-) ==
  return true;

op2 : nat * seq of nat ==> bool
op2(-,-) ==
  return true

functions

fn : nat * nat -> bool
fn(-,-) ==
  true;

fn : nat * real -> bool
fn(-,-) ==
  true;

fn2 : nat * nat -> bool
fn2(-,-) ==
  true;

fn2 : nat * seq of nat -> bool
fn2(-,-) ==
  true

end A

