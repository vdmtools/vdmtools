-- This file has been automatically updated wrt. VDM++ Lang.Adjust, 1th iteration
public Test : () ==> seq of ( (map char to nat) |
                          (map nat to (map nat to nat | seq of nat)) |
                          (seq of seq of nat) )  
Test () ==
        (dcl b: map nat to (map nat to nat| seq of nat) := {3|->{4|->23}},
             c: seq of seq of nat := [[1,3]];
         b(3)(4) := 117;
         c(1)(2) := 34;
         return [b,c])
functions
f : () -> int
f () ==
  24