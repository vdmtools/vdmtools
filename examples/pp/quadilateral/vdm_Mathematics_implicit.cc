// Hand written implementations of implicit functions in class
// Mathematics.

Real vdm_Mathematics::vdm_sqrt(Real r)
{
  Real res(sqrt(r.GetValue()));
  return res;
}

Real vdm_Mathematics::vdm_acos(Real r)
{
  Real res(acos(r.GetValue()));
  return res;
}
